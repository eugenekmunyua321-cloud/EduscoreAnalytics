reportlab>=3.6.0
Pillow>=9.0.0
requests>=2.28.0
phonenumbers>=8.12.0
# Removed Firebase dependencies (local auth/storage used)

firebase-admin>=6.0.0
google-cloud-storage>=2.0.0
# Database support for Render Postgres
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
# AWS S3 SDK
boto3>=1.26.0
# Note: Firebase libs are optional. If you don't use Firebase, these can be removed.
